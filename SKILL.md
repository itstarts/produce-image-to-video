---
name: produce-image-to-video
description: 编排通用的旁白驱动图生视频项目：用中文向用户澄清需求并给出可选择的推荐方案，使用 Codex 内置 Image Gen 制定风格、生成角色锚点和分镜图，引导用户在任意外部图生视频平台手动生成并保存片段，再完成素材验收、旁白同步、字幕、合成和最终验证。用于故事、科普、产品、人物、纪录片等从文案或已有音频开始的视频制作；不绑定 Gemini 或任何单一平台。
---

# Produce Image To Video

把 Codex 作为制片协调者：Codex 负责中文旁白、Image Gen 生图、提示词、检查与本地后期；用户负责关键创意选择、提供或录制音频，并在自己选择的外部平台执行图生视频。

## 核心规则

- 默认使用中文沟通、写旁白、写 Image Gen 提示词和图生视频提示词。文件名、路径、模型名及平台强制参数可保留英文。
- 不把推荐方案当作用户批准。涉及内容、风格、角色、分镜、旁白锁定、外部平台、保存路径和瑕疵取舍时，等待用户明确选择。
- 每次只问当前阶段必需的 1～3 个问题，并提供 2～3 个互斥方案、一个有依据的推荐方案，以及用户自定义入口。详见 [references/decision-protocol.md](references/decision-protocol.md)。
- 不固定题材、画风、镜头数、视频平台、片段时长或输出目录。
- Image Gen 的可用性以当前任务实际工具列表为准。Skill 文件、功能标志或旧会话成功记录都不能证明当前会话可生图。
- 默认只使用内置 Image Gen。若当前任务没有该工具，明确说明没有生成图片；建议新任务重试。未经用户单独批准，不切换到需要 `OPENAI_API_KEY` 或可能计费的 CLI/API 路径。
- 每个不同分镜使用一次独立 Image Gen 调用；不要用一个多变体请求替代多场景生成。
- 视觉质量是独立验收门禁。先确认风格和角色，再生成代表性分镜；代表性分镜通过后才生成其余图片。
- 外部图生视频平台由用户选择。先提供平台中立提示词，再在已知平台时添加经核实的适配说明；不要臆造平台参数。
- 用户保存的每个片段都要检查后再进入下一幕。不要因为文件存在就声明合格。
- 不发布、不上传成片、不使用付费服务、不改变账号或平台设置，除非用户对具体操作另行授权。

## 启动与恢复

1. 搜索当前项目的 `video-project.json`。
2. 找到后读取它并从第一个未完成门禁继续；不要重复已确认阶段。
3. 没有状态文件时，询问项目目录。推荐使用独立项目目录，但允许用户指定任何可访问位置。
4. 在用户批准创建项目后运行：

```bash
python <skill-dir>/scripts/init_project.py <project-dir> --title "<项目名称>"
```

5. 每次用户确认关键决定后，更新 `video-project.json` 中当前选择和状态。只记录最终有效决定，不保存被推翻的候选。

项目结构与字段见 [references/project-state.md](references/project-state.md)。

## 工作流路由

### 已有音频

先检查音频、转写或核对文本、检测时长和停顿，再拆镜头。随后进入风格制定、Image Gen 生图和外部图生视频。

### 没有音频

先生成并批准旁白初稿，再制定风格、拆镜头和生图。所有分镜确认后锁定旁白文字，请用户录制或提供音频；收到实际音频后重新计算准确镜头时长，才交付最终图生视频提示词。

完整顺序、门禁和回退路径见 [references/workflow.md](references/workflow.md)。

## 阶段门禁

### 1. 需求与旁白

- 确认主题、目标、观众、平台、画幅、预期时长和语气。
- 根据选择写中文旁白初稿并估算朗读时长。
- 用户批准旁白内容后才拆分叙事段落；此时旁白仍可因分镜小幅调整，尚未要求录音。

### 2. 风格与视觉锚点

- 没有参考时，基于内容推荐 2～3 个真正不同的视觉方向，并说明图生视频稳定性和制作成本。
- 有参考图时，提取媒介、造型、构图、配色、光线、纹理和禁用项；让用户确认提取结果。
- 有品牌规范时，以品牌规范为上限，不擅自改变品牌风格。
- 用相同代表性内容生成风格候选。用户选定后写入 `style_profile`。
- 需要固定人物、产品或品牌主体时，先生成并批准角色或产品锚点。

生图提示结构和一致性规则见 [references/image-and-style.md](references/image-and-style.md)。

### 3. 分镜图

- 根据旁白段落建立镜头清单，确保一幕只承担一个主要信息。
- 先生成一张代表性分镜。用户确认构图、人物和风格后，再逐幕生成其余图片。
- 每张最终图片复制到项目 `images/scenes/scene-XX.png`，不要只留在 Image Gen 默认目录。
- 不覆盖已确认图片；修订使用 `scene-XX-v2.png`，确认后更新状态文件引用。

### 4. 锁定旁白并收音频

- 全部分镜确认后做最后一次旁白—画面一致性检查。
- 让用户批准“锁定旁白”。锁定后才要求用户按该版本录音或提供最终音频，避免反复重录。
- 音频到达后检查能否解码、真实时长、声道、采样率、静音区和明显削波；核对朗读内容是否与锁定文字一致。
- 用实际音频停顿修正每幕目标时长。若平台片段时长不够，优先拆幕；安全的结尾静帧补足只用于没有新叙事信息的尾段。

### 5. 外部图生视频

- 询问用户选择的平台。用户没有偏好时可以推荐，但说明依据；不要把推荐平台当作批准。
- 询问保存位置。推荐 `<project>/inputs/generated-clips/`，也接受用户给出的目录或具体文件路径。
- 每次只交付一幕：分镜图路径、目标时长、平台中立中文提示词、平台适配说明、文件名和保存位置。
- 用户回复完成后立即检查文件；合格后才交付下一幕。

提示词格式、平台适配和重做方式见 [references/video-prompts.md](references/video-prompts.md)。

### 6. 后期与交付

- 所有片段通过后，根据实际旁白时长裁切、补帧、统一分辨率和帧率，再拼接。
- 可用 `scripts/assemble_video.py` 按项目清单生成基础成片；运行前确认清单里的目标时长已经锁定。
- 根据锁定旁白和实际停顿生成中文字幕。默认无背景、白字、细深色描边、底部安全区；用户可以选择其他方案。
- 混入旁白时保留原始录音文件；除非用户批准，不擅自增加音乐或音效。
- 对最终文件执行完整解码、流信息、时长、音量和抽帧检查；在用户视觉确认前，不声称已经发布或最终验收。

质量门禁和验收项见 [references/quality-gates.md](references/quality-gates.md)。

## 工具与资源

- `scripts/init_project.py`：创建不覆盖现有文件的项目目录和状态文件。
- `scripts/validate_media.py`：检查媒体流、时长、规格、完整解码和可选帧变化。
- `scripts/assemble_video.py`：按锁定镜头时长统一、裁切、补帧、拼接并合入旁白。
- `assets/project-template.json`：通用项目状态模板。
- `references/`：只在进入相应阶段时读取，避免一次加载全部细节。

## 完成声明

交付时区分以下状态：图片已生成、图片已确认、片段已保存、片段自动检查通过、片段人工确认、成片已合成、成片自动验证通过、成片人工确认。不要把任一状态替代另一个状态。
